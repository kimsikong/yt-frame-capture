# 유튜브 장면 캡쳐 확장 프로그램 — 설치 가이드

유튜브에서 보고 있는 장면을 **버튼 한 번**으로 원본 해상도 이미지로 저장합니다.
화면 캡쳐와 달리 자막·플레이어 UI·검은 여백이 안 섞이고, 영상 프레임만 깨끗하게 나옵니다.

> 설치 링크: **https://github.com/kimsikong/yt-frame-capture**
> Chrome / Edge / Brave 등 크로미움 기반 브라우저에서 동작합니다. Safari·Firefox는 지원하지 않습니다.

---

## 설치 (3분)

**1. 파일 받고 압축 풀기**

위 링크에서 ZIP을 받아 압축을 풉니다.
압축을 푼 폴더를 **다운로드 폴더가 아닌 고정된 위치**로 옮기세요 (예: `문서` 폴더).
👉 이 폴더를 나중에 지우거나 옮기면 확장이 멈춥니다. 브라우저가 이 폴더를 계속 참조합니다.

**2. 확장 프로그램 페이지 열기**

주소창에 입력: `chrome://extensions`

**3. 개발자 모드 켜기**

오른쪽 위 **개발자 모드** 토글을 켭니다.

**4. 폴더 선택**

**압축해제된 확장 프로그램을 로드합니다** 클릭 → 1번에서 옮긴 폴더 선택.

> ⚠️ 가장 흔한 실수: GitHub에서 받으면 폴더 이름이 `yt-frame-capture-main` 입니다.
> 그 안에 `manifest.json`이 바로 보여야 맞습니다. 폴더가 두 겹이면 안쪽을 고르세요.
> 한 단계 위를 선택하면 "매니페스트 파일이 없습니다" 오류가 납니다.

끝입니다. 열려 있던 유튜브 탭은 자동으로 새로고침됩니다.

---

## 사용법

| 하고 싶은 것 | 방법 |
|---|---|
| 지금 이 장면 저장 | 플레이어 오른쪽 아래 **카메라 버튼 클릭** |
| 클립보드로 복사 (바로 붙여넣기) | 카메라 버튼 **Shift + 클릭** |
| 최근 캡쳐 보기 | 브라우저 툴바의 빨간 카메라 아이콘 |
| 설정 변경 | 툴바 아이콘 → 설정 열기 |

**정확한 장면을 잡는 법**
**K** 로 일시정지 → **,** 와 **.** 로 한 프레임씩 앞뒤 이동 → 카메라 버튼 클릭.
(`K` `,` `.` 는 유튜브 자체 기능입니다.)

저장 위치: `다운로드 / YouTube Captures / 영상제목 [영상ID] 00-03-27.480.png`
파일명 형식, 폴더, 파일 형식(PNG·JPEG·WebP)은 설정에서 바꿀 수 있습니다.

---

## 화질에 대해

**캡쳐 해상도 = 그 순간 재생 중인 화질**입니다. 1080p로 보고 있어야 1920×1080이 나옵니다.

더 크게 뽑고 싶다면: 플레이어 설정(⚙️)에서 화질을 먼저 올리고 → 그 화질로 몇 초 재생시켜 버퍼링한 뒤 → 캡쳐하세요.
화질을 올리자마자 바로 캡쳐하면 아직 이전 화질 프레임이 나올 수 있습니다.

---

## 안 될 때

| 증상 | 해결 |
|---|---|
| 카메라 버튼이 안 보임 | 유튜브 탭을 새로고침 (`Cmd/Ctrl + R`). 마우스를 영상 위에 올려야 컨트롤 줄이 나타납니다 |
| 버튼을 눌러도 반응 없음 | 유튜브 탭을 새로고침. 그래도 안 되면 툴바 아이콘 → 지금 캡쳐 |
| "프레임을 읽을 수 없습니다" 오류 | DRM 보호 영상(유튜브 영화·대여 콘텐츠)입니다. 브라우저가 막는 것이라 우회 불가 |
| `chrome://extensions`에 오류 표시 | 폴더를 잘못 선택했을 가능성이 큽니다. 위 4번의 ⚠️ 참고 |
| 갑자기 확장이 사라짐 | 압축 푼 폴더를 지웠거나 옮겼는지 확인 |

브라우저를 켤 때마다 **"개발자 모드 확장 프로그램을 사용 중지하세요"** 풍선이 뜰 수 있습니다.
개발자 모드로 설치한 확장의 정상적인 경고이며, **닫기**를 누르면 그대로 동작합니다.

---

## 알아두면 좋은 것

- 이 확장은 **인터넷으로 아무것도 보내지 않습니다.** 캡쳐는 브라우저 안에서 끝나고 파일은 내 컴퓨터에만 저장됩니다.
- 접근 권한은 `youtube.com` 과 다운로드 기능뿐입니다.
- 코드는 전부 열려 있으니 설치 전에 직접 읽어보셔도 됩니다.
- 캡쳐한 이미지를 다시 올릴 때는 원 영상 출처를 함께 밝히는 편이 안전합니다.
